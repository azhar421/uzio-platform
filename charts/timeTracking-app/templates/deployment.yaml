apiVersion: apps/v1
kind: Deployment
metadata:
  name: timetracking-app
  labels:
    app: timetracking-app
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timetracking-app
  template:
    metadata:
      labels:
        app: timetracking-app
    spec:
      containers:
        - name: timetracking-app
          image: hashicorp/http-echo
          args:
            - "-text=timetracking-app running"
          ports:
            - containerPort: 5678
